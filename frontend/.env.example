# Vite: only variables prefixed with VITE_ are exposed to the client.
# Shared TS types live in repo /shared; imports use alias @shared (see tsconfig + vite.config).

VITE_API_BASE_URL=http://localhost:8000
VITE_GATEWAY_WS_URL=ws://localhost:8000/ws/telemetry/
VITE_ML_BASE_URL=http://127.0.0.1:8001
